library verilog;
use verilog.vl_types.all;
entity max_pool is
    port(
        din             : in     vl_logic_vector(63 downto 0);
        dout            : out    vl_logic_vector(15 downto 0)
    );
end max_pool;
